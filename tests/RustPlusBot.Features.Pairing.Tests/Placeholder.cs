namespace RustPlusBot.Features.Pairing.Tests;

/// <summary>Placeholder so the test project compiles before real tests are added in later tasks.</summary>
public sealed class Placeholder
{
    /// <summary>Ensures the project compiles.</summary>
    [Fact]
    public void ProjectCompiles() => Assert.True(true);
}
